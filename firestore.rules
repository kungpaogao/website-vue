rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  	// anyone can read from "doing"
    match /doing/{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}